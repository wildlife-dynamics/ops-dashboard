[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wd-tasks"
version = "0.1.0"
requires-python = ">=3.10"

[project.entry-points."wt_registry"]
wd-tasks = "wd_tasks"
